# Core: always required
numpy==2.5.1
PyYAML==6.0.3
requests==2.34.2

# Audio: microphone capture, voice activity detection, speech recognition
sounddevice==0.5.5
webrtcvad==2.0.10
faster-whisper==1.2.1
openwakeword==0.6.0

# Text-to-speech
pyttsx3==2.99
edge-tts==7.2.8
playsound3==3.3.2

# UI
PySide6==6.11.1

# Skills
psutil==7.2.2
mss==10.2.0
pyperclip==1.11.0

# Vision: screenshots and OCR (requires Tesseract OCR installed)
pillow==12.3.0
pytesseract==0.3.13

# Camera and face recognition (needs CMake + C++ compiler for dlib)
# Uncomment if you use skills: add face, who is in front of camera
# opencv-python==4.11.0.86
# face_recognition==1.3.0

# Long-term memory with semantic search
chromadb==1.5.9

# Browser automation (after install: playwright install chromium)
playwright==1.61.0

# Spotify control (needs SPOTIFY_CLIENT_ID and SPOTIFY_CLIENT_SECRET)
spotipy==2.26.0

# Cloud brain (not needed when backend: ollama)
openai==2.51.0

# Windows only: window management and precise volume control
pygetwindow==0.0.9; sys_platform == "win32"
pycaw==20251023; sys_platform == "win32"
comtypes==1.4.16; sys_platform == "win32"
